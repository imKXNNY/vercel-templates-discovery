# ADOS Review Gate Report — M3 Issues #5 and #6

## Summary

Implemented M3 deliverables: Hermes skill wrapper (Issue #5) and REST API server mode (Issue #6).

## Changes

- Added `vercel_templates/server.py` — FastAPI app with dependency-injected scraper.
- Added `vercel_templates/cli.py::serve` command — starts uvicorn.
- Added `skills/vercel-templates/SKILL.md` and `skills/vercel-templates/scripts/query.py`.
- Added `tests/test_server.py` covering `/health`, `/templates`, `/templates/{slug}`, `/categories`.
- Updated `pyproject.toml` with `fastapi`, `uvicorn`, `httpx` in `dev` and `server` extras.
- Updated `README.md` with REST server usage.
- Updated `CHANGELOG.md` and `docs/PROJECT_STATUS.md`.

## Verification

- `ruff check .` — pass
- `ruff format --check .` — pass
- `mypy vercel_templates` — pass (with `disallow_untyped_decorators = false` override for `server.py`)
- CI test suite (Python 3.10/3.11/3.12) — pass
- GitGuardian — pass

## Gate result

**Gate:** Review  
**Status:** PASS  
**Next:** proceed to M4/M5 backlog
